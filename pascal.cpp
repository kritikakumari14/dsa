#include<iostream>
using namespace std;
vector<vector<int>> generate(int numRows) {
    int arr[numRows][numRows];
       for(auto i:numRows){
            
       } 
    }
int main(){
    int n=5;
    generate(n);
    
}